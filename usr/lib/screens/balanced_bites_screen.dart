import 'package:flutter/material.dart';

class BalancedBitesScreen extends StatelessWidget {
  const BalancedBitesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Balanced Bites AI - Meal Planner',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
